
# (1,1 MB birikmişti — 2026-08-07'de temizlendi).
# Bulgu kalıcıysa `gunluk/` altına YAZILIR; ham çıktı repoda tutulmaz
# MCP yapılandırması — içinde Qdrant API key'i düz duruyor, git'e girmez.
# Panelin çalışma çıktısı — süreç üretiyor, kayıt değil
# Tek seferlik çıktılar — bir kez üretilir, bir kez bakılır, kayıt değildir.
# Şablonu: .mcp.json.example
*-kanal.png
.DS_Store
.mcp.json
ekran-*.png
panel/durum.json
panel/topla.log
session-report-*.html
.tmp-cu-token
.tmp-cu-watch.py

# Silinen dosyalar — geri alinabilir, git'e gitmez (Mert'in karari 2026-09-03:
# silinecek her sey silinmez, repo kokundeki trash/ altina tasinir)
trash/

# Artifact HTML kaynakları — yayınlanan sayfa asıl, dosya ara ürün
.artifacts/

# Remember plugin kendi kaydini tutuyor — surec ciktisi, kayit degil
.remember/

# Playwright sayfa dokumleri — tek seferlik
.playwright-mcp/

# Ekran goruntusu artiklari
colab-*.png
